name: Daily volume momentum notification

on:
  schedule:
    - cron: "0 11 * * 1-5"
  workflow_dispatch:
    inputs:
      limit:
        description: "Limit tickers for smoke testing. Empty means all tickers."
        required: false
        default: ""
      send_email:
        description: "Send Gmail notification."
        required: true
        type: boolean
        default: false

jobs:
  daily-notify:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Restore SQLite and yfinance cache
        uses: actions/cache@v4
        with:
          path: |
            data/volume_momentum.sqlite3
            data/yfinance_cache
          key: volume-momentum-cache-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            volume-momentum-cache-${{ runner.os }}-

      - name: Install dependencies
        run: python -m pip install -e .

      - name: Run tests
        run: python -m unittest discover -s tests
        env:
          PYTHONPATH: src

      - name: Run daily notification
        shell: bash
        env:
          GMAIL_SMTP_USER: ${{ secrets.GMAIL_SMTP_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          MAIL_CC: ${{ secrets.MAIL_CC }}
          MAIL_BCC: ${{ secrets.MAIL_BCC }}
          INPUT_LIMIT: ${{ github.event.inputs.limit }}
          INPUT_SEND_EMAIL: ${{ github.event.inputs.send_email }}
        run: |
          args=(--config config.example.json daily-notify --download --market-caps)
          limit_value="${INPUT_LIMIT#limit=}"
          if [[ -n "$limit_value" ]]; then
            if [[ ! "$limit_value" =~ ^[0-9]+$ ]]; then
              echo "limit must be an integer. Received: $INPUT_LIMIT" >&2
              exit 2
            fi
            args+=(--limit "$limit_value")
          fi
          if [[ "${{ github.event_name }}" == "schedule" || "$INPUT_SEND_EMAIL" == "true" ]]; then
            args+=(--send-email)
          else
            args+=(--dry-run)
          fi
          python -m volume_momentum.cli "${args[@]}"

      - name: Upload daily reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: daily-volume-momentum-reports
          path: reports/daily
          if-no-files-found: ignore
